import * as React from "react"
import HoverText from "./hoverbase"

const WorkMyEverestTrip = () => {
  return (
    <HoverText
      title={`Lovebit`}
      description={`Website Redesign.`}
      linkColor={`var(--brand)`}
      bgColor={`#8EDC97`}
      link={`/dwt/`}
    >
      <span>Something</span>
    </HoverText>
  )
}
export default WorkMyEverestTrip