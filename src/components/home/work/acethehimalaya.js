import * as React from "react"
import HoverText from "./hoverbase"

const WorkAceTheHimalaya = () => {
  return (
    <HoverText
      title={`Ace the Himalaya`}
      description={`Strategy. UX Design. Development.`}
      linkColor={`var(--brand)`}
      bgColor={`#8EDC97`}
      link={`/dwt/`}
    >
      <span>Something</span>
    </HoverText>
  )
}

export default WorkAceTheHimalaya
