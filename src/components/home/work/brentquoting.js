import * as React from "react"
import HoverText from "./hoverbase"

const WorkBrentQuoting = () => {
  return (
    <HoverText
      title={`Brent Quoting`}
      description={`UX Design.`}
      linkColor={`var(--brand)`}
      bgColor={`var(--brand)`}
      link={`/brent-quoting-system/`}
    >
      <h1>something</h1>
    </HoverText>
  )
}

export default WorkBrentQuoting