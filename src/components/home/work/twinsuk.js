import * as React from "react"
import HoverText from "./hoverbase"

const WorkTwinsuk = () => {
  return (
    <HoverText
      title={`TwinsUK`}
      description={`Information Architecture. UX Design.`}
      linkColor={`var(--brand)`}
      bgColor={`purple`}
      link={`/travel-max/`}
    >
      <h1>something</h1>
    </HoverText>
  )
}
export default WorkTwinsuk