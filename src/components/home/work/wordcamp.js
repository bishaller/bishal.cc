import * as React from "react"
import HoverText from "./hoverbase"

const WorkWordCamp = () => {
  return (
    <HoverText
      title={`WordCamp Ktm`}
      description={`Strategy. Print Design.`}
      linkColor={`var(--brand)`}
      bgColor={`blue`}
      link={`/wordcamp-kathmandu/`}
    >
      <h1>something</h1>
    </HoverText>
  )
}
export default WorkWordCamp