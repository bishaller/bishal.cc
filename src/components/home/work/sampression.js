import * as React from "react"
import HoverText from "./hoverbase"

const WorkSampression = () => {

  return (
    <HoverText
      title={`Sampression`}
      description={`Ideation. Product Design.`}
      linkColor={`var(--brand)`}
      bgColor={`red`}
      link={`/dwt/`}
    >
      <span>Something</span>
    </HoverText>
  )
}

export default WorkSampression
