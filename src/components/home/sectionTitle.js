import * as React from "react"

const SectionTitle = () => {
  return (
    <section className="SectionTitle">
      <div className="siteContainer siteContainer--medium">
        <h2 className="SectionTitle__title">work</h2>
      </div>
    </section>
  )
}

export default SectionTitle
